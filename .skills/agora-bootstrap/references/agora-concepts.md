# Agora Concepts

- Agora Core is the orchestration truth source.
- IM threads or groups are collaboration containers, not the source of task truth.
- `controller` owns stage progression and coordination.
- `citizen` agents discuss or execute within allowed stage semantics.
- `craftsman` executors are dispatched only from craftsman-eligible stages.
- Human approval stages cannot be bypassed by agent action.
