# Roles

Canonical role documents live here as briefing sources.

Expected format:

- YAML frontmatter for machine-readable briefing extraction
- markdown body for human/agent-readable detail

Task-specific role briefs should be materialized under:

- `tasks/<task-id>/05-agents/<agent-id>/00-role-brief.md`
