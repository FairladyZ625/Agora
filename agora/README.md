# Agora Python Legacy Archive

这个目录保留为 Python 参考实现与历史对照，不再是默认开发或运行目标。

当前约定：

- 默认实现口径是 `/Users/lizeyu/Projects/Agora/agora-ts`
- Dashboard 前端仍在 `/Users/lizeyu/Projects/Agora/dashboard`
- `agora-ts` 已不再依赖本目录内的 `templates` 或 `config/agora.example.json`

使用边界：

- 仅在需要对照 legacy Python 行为时阅读或启动
- 不再向这里新增 TS 主线能力
- 新的模板与 config example 资产以 `agora-ts` 内部版本为准
